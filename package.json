{
  "name": "portrait-sdk",
  "version": "0.0.4",
  "repository": "git@github.com:portraitgg/portrait-sdk.git",
  "author": "Ryan Shahine <ryan@portrait.gg>",
  "license": "MIT",
  "scripts": {
    "compile": "rm -rf dist/lib && tsc && tsc --build tsconfig.es5.json",
    "es5": "rm -rf dist/lib && tsc --build tsconfig.es5.json",
    "es6": "rm -rf dist/lib && tsc"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "ethers": "^6.0.3"
  }
}
